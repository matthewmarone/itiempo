import React from "react";
import PropTypes from "prop-types";

/**
 *
 * @param {*} props
 * @returns
 */
const Verse = (props) => {
  return <span>Verse will be here</span>;
};
Verse.propTypes = {};

export default Verse;
